# Force Linux platform for Netlify builds
platform=linux
arch=x64

# Exclude optional dependencies
optional=false

# Force platform-specific packages
target_platform=linux
target_arch=x64

# Ensure proper dependency resolution
legacy-peer-deps=true
